package es.daw.sostenibilidad.energia;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Observatorio del reto energético. Arranca con {@code ./mvnw spring-boot:run}. */
@SpringBootApplication
public class ObservatorioEnergiaApplication {

    public static void main(String[] args) {
        SpringApplication.run(ObservatorioEnergiaApplication.class, args);
    }
}
