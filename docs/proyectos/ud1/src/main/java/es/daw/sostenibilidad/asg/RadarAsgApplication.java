package es.daw.sostenibilidad.asg;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Punto de entrada del Radar ASG. Arranca con {@code ./mvnw spring-boot:run}. */
@SpringBootApplication
public class RadarAsgApplication {

    public static void main(String[] args) {
        SpringApplication.run(RadarAsgApplication.class, args);
    }
}
